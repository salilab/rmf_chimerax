sudo: false
language: python
dist: xenial
python:
  - 3.8
cache:
  pip: true
install:
  - pip install coverage
script:
  - nosetests --with-coverage --cover-branches
after_success:
  - bash <(curl -s https://codecov.io/bash)
